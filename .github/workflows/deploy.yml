name: Continuous Deployment

on:
  push:
    branches:
      - master

concurrency:
  group: cd-deployment
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@83083e9515518b0222a7f5a43924f114099b0c53
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            set -e
            cd ~/jobetl
            git fetch origin
            git reset --hard origin/master
            npm ci
            npm run db:migrate
            npm run build
            # Reload PM2 application to apply code updates
            npx pm2 reload ecosystem.config.cjs --env production || npx pm2 start ecosystem.config.cjs --env production
